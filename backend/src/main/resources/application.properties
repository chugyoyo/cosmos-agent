spring.application.name=cosmos-agent

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/cosmos_agent
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# MyBatis-Plus Configuration
mybatis-plus.configuration.map-underscore-to-camel-case=true
mybatis-plus.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl
mybatis-plus.mapper-locations=classpath:mapper/*.xml
mybatis-plus.global-config.db-config.table-prefix=
mybatis-plus.global-config.db-config.id-type=auto

# Server Configuration
server.port=8080

# Spring AI Configuration
spring.ai.openai.api-key=${OPENAI_API_KEY:92d02c6d9b2f462fbea498b231b0914c.FmtzV1pzFeqrarE0}
spring.ai.openai.base-url=https://api.openai.com

# CORS Configuration
spring.mvc.cors.allowed-origins=*
spring.mvc.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.mvc.cors.allowed-headers=*
